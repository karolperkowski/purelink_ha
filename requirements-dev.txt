# Dev dependencies. Loose pins on HA stack (lots of patch releases) and a hard pin on
# ruff (frequent breaking changes). Keep this file as the single source CI and local devs install from.
homeassistant>=2025.1.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-homeassistant-custom-component>=0.13.0
ruff==0.7.4
