[pytest]
testpaths = tests
# Required by pytest-homeassistant-custom-component: the hass fixture and
# tests are async and must be collected/awaited automatically.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
